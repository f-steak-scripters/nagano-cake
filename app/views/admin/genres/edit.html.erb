<div class="container m-5">
  <div class="row">
    <h4 class="title ml-5 bg-light text-dark">ジャンル編集</h4>
  </div>
  <div class="row mt-5 ml-3">
    <div class="fild mb-5">
    <%= form_for(@genre, url: admin_genre_path) do |f| %>
      <%= f.label :name, "ジャンル名", class: "mr-5 font-weight-bold" %>
      <%= f.text_field :name, placeholder: "ジャンル名" ,class: "mr-5"%>
      <%= f.submit "変更を保存", class: "btn btn-success px-3 pl-4 pr-4 ml-5" %>
    <% end %>
    </div>
  </div>